application: starlit-tine-552
module: news-module
version: uno
runtime: python27
api_version: 1
threadsafe: true

instance_class: B1
basic_scaling:
  max_instances: 1
  idle_timeout: 5m

handlers:
- url: /makenews
  script: makenews.APP